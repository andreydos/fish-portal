import NavMenuAdditional from './nav-bar-additional'

describe('@components/nav-menu-additional', () => {
  it('exports a valid component', () => {
    expect(NavMenuAdditional).toBeAComponent()
  })
})
